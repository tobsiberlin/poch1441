# Card Motion Plane Lock V1

Isolated native Track-B proof for one card only. The product App and all other review folders are read-only inputs.

Run:

```sh
./Scripts/run.sh
```

The script executes the unit tests, launches the iPhone 17 Pro simulator, records real-wallclock evidence and copies it into `Evidence/`.
